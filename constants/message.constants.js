const DATABASE_CONNECTED = "Connected to the database.";

const DATABASE_CONNECTION_ERROR = "Error while connecting to the database";

const CREATE_TABLE_USERS_ERROR = "Error while creating table users";

const CREATE_TABLE_EXERCISES_ERROR = "Error while creating table exercises";

const USER_NOT_FOUND = "User with the given ID does not exist";

const USERNAME_REQUIRED = "Username is required";

const USERNAME_ALREADY_EXISTS = "Username already exists";

const TABLE_USERS_EMPTY = "Table users is empty";

const DESCRIPTION_AND_DURATION_REQUIRED =
  "Description and duration are required";

const DESCRIPTION_REQUIRED = "Description is required";

const DURATION_REQUIRED = "Duration is required";

const DURATION_MUST_BE_POSITIVE_NUMBER = "Duration must be positive number";

const INVALID_DATE = "Invalid date";

module.exports = {
    DATABASE_CONNECTED,
    DATABASE_CONNECTION_ERROR,
    CREATE_TABLE_USERS_ERROR,
    CREATE_TABLE_EXERCISES_ERROR,
    USER_NOT_FOUND,
    USERNAME_REQUIRED,
    USERNAME_ALREADY_EXISTS,
    TABLE_USERS_EMPTY,
    DESCRIPTION_AND_DURATION_REQUIRED,
    DESCRIPTION_REQUIRED,
    DURATION_REQUIRED,
    DURATION_MUST_BE_POSITIVE_NUMBER,
    INVALID_DATE,
  };